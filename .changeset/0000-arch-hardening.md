---
"utilitylab": minor

Architectural hardening: Stripe webhook idempotency guard, Zod request validation on all JSON routes, 10 MB file-size gate, env-var validation at cold start, atomic rate-limit RPC (eliminates race condition), withApiHandler wrapper, standard error envelope, OCR service abstraction, health endpoint, React error boundaries, structured logging in OCR providers, Vitest unit/integration tests (74 tests), Playwright E2E specs, and components/ui/ wrapper layer.
