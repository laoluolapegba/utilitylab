"use client";

/**
 * Inline Alert / notification strip.
 * Wraps Flowbite React's Alert so product components are
 * decoupled from the underlying library.
 */

export { Alert } from "flowbite-react";
