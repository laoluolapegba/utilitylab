"use client";

/**
 * Status / label Badge.
 * Thin wrapper over Flowbite React's Badge so product components
 * are decoupled from the underlying library.
 */

export { Badge } from "flowbite-react";
